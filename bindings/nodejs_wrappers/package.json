{
  "name": "dogecoinjs",
  "version": "0.1.0",
  "description": "dogecoinjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "gypfile": true,
  "scripts": {
    "build": "node-gyp configure && node-gyp build && tsc --build",
    "clean": "node-gyp clean && tsc --build --clean",
    "test": "npm run clean && npm run build && ts-node test"
  },
  "devDependencies": {
    "@types/bindings": "1.5.1",
    "@types/node": "^17.0.23",
    "bindings": "~1.5.0",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.3"
  }
}
